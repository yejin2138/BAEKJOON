#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int num,count=1,val=1;
	scanf("%d", &num);
	while (1) {
		val += 6 * count;
		if (num == 1) {
			printf("1");
			break;
		}

		if (num <= val) {
			printf("%d", count+1);
			break;
		}
		count++;
	}
}
