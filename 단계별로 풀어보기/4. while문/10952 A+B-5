
#include<stdio.h>
int main()
{
	int a,b,count=1, num=1, i,j;
	while (count > 0 && num>0)
	{
		scanf("%d %d", &count, &num);
		if (count + num != 0)
		{
			printf("%d\n", count + num);
		}
		else break;
	}
	return 0;
}

